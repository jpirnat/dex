{% extends "html/base.twig" %}

{% block body %}
	<div class="month-control">
		{% if prevMonth.show %}
			<div class="button prev">
				<a href="/stats/{{ prevMonth.month|e }}">
					&#9668; {{ prevMonth.text|e }}
				</a>
			</div>
		{% endif %}

		<div class="space"></div>

		{% if nextMonth.show %}
			<div class="button next">
				<a href="/stats/{{ nextMonth.month|e }}">
					{{ nextMonth.text|e }} &#9658;
				</a>
			</div>
		{% endif %}
	</div>

	<table>
		<thead>
			<tr>
				<th>Format</th>
				<th>Rating 1</th>
				<th>Rating 2</th>
				<th>Rating 3</th>
				<th>Rating 4</th>
			</tr>
		</thead>
		<tbody>
			{% for format in formats %}
				<tr>
					<td>{{ format.name }}</td>
					{% for rating in format.ratings %}
						<td>
							<a href="/stats/{{ month|e }}/{{ format.identifier|e }}/{{ rating|e }}">
								{{ rating|e }}
							</a>
						</td>
					{% endfor %}
				</tr>
			{% endfor %}
		</tbody>
	</table>
{% endblock %}
